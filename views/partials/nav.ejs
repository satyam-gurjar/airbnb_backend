<!-- Header -->
<header class="bg-white border-b sticky top-0 z-50">
  <div class="max-w-7xl mx-auto px-6 py-4 flex justify-between items-center">

    <!-- Logo -->
    <h1 class="text-2xl font-extrabold text-red-500 tracking-tight">
      Airbnb
    </h1>

    <!-- Navigation -->
    <nav class="space-x-8 text-gray-600 font-medium">

      <a href="/"
         class="transition
         <%= currentPage === 'Home'
              ? 'text-red-500 font-semibold'
              : 'hover:text-red-500' %>">
        Home
      </a>

      <!-- <a href="/about"
         class="transition
         <%= currentPage === 'About'
              ? 'text-red-500 font-semibold'
              : 'hover:text-red-500' %>">
        About
      </a>

      <a href="/contact"
         class="transition
         <%= currentPage === 'Contact'
              ? 'text-red-500 font-semibold'
              : 'hover:text-red-500' %>">
        Contact
      </a> -->

      <a href="/host/add-home"
         class="transition
         <%= currentPage === 'AddHome'
              ? 'text-red-500 font-semibold'
              : 'hover:text-red-500' %>">
        Add
      </a>

    </nav>
  </div>
</header>
